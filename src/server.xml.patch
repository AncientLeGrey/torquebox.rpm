# This patch file was generated by NetBeans IDE
# This patch can be applied using context Tools: Apply Diff Patch action on respective folder.
# It uses platform neutral UTF-8 encoding.
# Above lines and this line are ignored by the patching process.
--- a/jboss/server/default/deploy/jbossweb.sar/server.xml
+++ b/jboss/server/default/deploy/jbossweb.sar/server.xml
@@ -10,7 +10,7 @@
 
       <!-- A HTTP/1.1 Connector on port 8080 -->
       <Connector protocol="HTTP/1.1" port="${jboss.web.http.port}" address="${jboss.bind.address}" 
-         redirectPort="${jboss.web.https.port}" />
+         redirectPort="${jboss.web.https.port}" URIEncoding="UTF-8"/>
 
       <!-- Add this option to the connector to avoid problems with 
           .NET clients that don't implement HTTP/1.1 correctly 
@@ -19,7 +19,7 @@
 
       <!-- A AJP 1.3 Connector on port 8009 -->
       <Connector protocol="AJP/1.3" port="${jboss.web.ajp.port}" address="${jboss.bind.address}"
-         redirectPort="${jboss.web.https.port}" />
+         redirectPort="${jboss.web.https.port}" URIEncoding="UTF-8"/>
 
       <!-- SSL/TLS Connector configuration using the admin devl guide keystore
       <Connector protocol="HTTP/1.1" SSLEnabled="true" 
